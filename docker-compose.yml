services:
  refract:
    build: .
    ports:
      - "8899:8899"
    command: ["refract", "explore", "Earth", "--no-open"]
    environment:
      - REFRACT_CACHE_DIR=/data
    volumes:
      - refract-cache:/data

volumes:
  refract-cache:
